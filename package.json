{
  "name": "catalogo-desapegos-fitness",
  "version": "1.0.0",
  "private": true,
  "description": "Catálogo online (Vercel + Supabase) para roupas fitness seminovas",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "fotos": "node scripts/gerar-fotos.js",
    "publicar": "git add -A && git commit -m \"Atualiza catálogo\" && git push",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": "22.x"
  }
}
