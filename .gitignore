# Arquivos temporários do SQLite (o catalogo.db em si é versionado)
catalogo.db-wal
catalogo.db-shm
catalogo.db-journal

node_modules/
.DS_Store
Thumbs.db

# Senhas e acessos: NUNCA vão para o Git
.env
.env.*
!.env.exemplo
ACESSOS.md

# Banco SQLite antigo (só backup local; o banco agora é o Supabase)
catalogo.db
catalogo.db-*
.vercel

.vercel

# Estado local do Wrangler (R2 emulado, cache)
.wrangler/
worker/.wrangler/
